import { partition_equal_subset_sum } from "@gepick/top-100-liked/common";

const ans = partition_equal_subset_sum([1, 2, 5]);

// eslint-disable-next-line no-console
console.log("🚀 ~ ans:", ans);
